# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

name: First-pipeline

trigger:
- master

resources:
- repo: self

variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: 'ca610e97-31ce-4f38-ab86-ec029b71eef6'
  imageRepository: 'saitejaproject'
  containerRegistry: 'saitejacontainerregistry.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  tag: '$(Build.BuildId)'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: '$(Tag)'

- task: AzureCLI@2
  inputs:
    azureSubscription: 'Free Trial(7338b8ab-3b46-420e-becc-da044eebe12c)'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript:  imageTag = az acr repository show-tags -n Saitejacontainerregistry --repository saitejaproject --top 1